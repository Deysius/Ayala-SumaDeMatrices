#include<stdio.h>
#include<stdlib.h>
#include<time.h>
int main(){
int filas, columnas;
srand (time(NULL));
    printf("Ingrese el número de filas de la matriz: ");
    scanf("%d", &filas);
    printf("Ingrese el número de columnas de la matriz: ");
    scanf("%d", &columnas);
return 0;
}